# Mor Logistics Broker Bot — Python Dependencies
# Install: pip install -r requirements.txt

# Discord bot framework (slash commands via app_commands)
discord.py>=2.3.2

# ── Google AI / Vertex AI ─────────────────────────────────────────────────────
# Unified Google GenAI SDK — covers Gemini via Vertex AI and Google AI Studio.
# Replaces the previous 'anthropic' package.
google-genai>=1.16.0

# Google APIs (Sheets + auth shared with Gemini service account)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.128.0

# Environment variable loader
python-dotenv>=1.0.1

# Async HTTP (used internally by discord.py and google-genai)
aiohttp>=3.9.5

# ORM and Database
SQLAlchemy>=2.0.0
aiosqlite>=0.19.0

# OCR and Image Processing
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=10.0.0

# Twilio SMS / WhatsApp
twilio>=9.0.0

# Background task scheduling (temperature check-ins)
APScheduler>=3.10.0

# Async HTTP client (for downloading Twilio media)
httpx>=0.27.0
